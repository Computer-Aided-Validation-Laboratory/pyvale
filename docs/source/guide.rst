User Guide
=================================

What is pyvale?
---------------------------------

`pyvale` is a virtual engineering laboratory that allows you to analyse and optimise sensor deployment and experimental design. The motivation behind `pyvale` is to provide engineers with a software tool

How does pyvale work?
---------------------------------

`pyvale`

How do I get started with pyvale?
---------------------------------

To get familiar with the core concepts of `pyvale` start with the :ref:`basic examples <examples_basics>`. With the core functionality of `pyvale` you will

After that you might want to look at some of the camera sensor simulation tools in `pyvale`.

To use `pyvale` with your own input physics simulation you will need

